import React from 'react'

import { Text } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';
import ChatScreen from '../pages/ChatPage';

const RightDrawerCustom = () => {
    return (
        <ChatScreen />
    );
}

export default RightDrawerCustom